services:
    app:
        build:
            context: .
            dockerfile: Dockerfile
        container_name: monaco-locacao
        restart: unless-stopped
        environment:
            APP_ENV: production
            APP_DEBUG: "false"
        networks:
            - coolify

networks:
    coolify:
        external: true
